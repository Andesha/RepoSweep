# RepoSweep

An agentic approach to cleaning up issues and PRs on your project.

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues (Andesha/RepoSweep) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical five-role vocabulary, label strings unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
